export enum RenderLayer {
  One = 1,
  Two = 2,
  Three = 3,
  Four = 4,
  Five = 5,
  Background = 1000,
}

export enum DEFAULT_CANVAS_SIZE {
  Width = 760,
  Height = 760,
}

export enum GameObjectTypes {
  Rectangle = 'Rectangle',
  Image = 'Image',
  Text = 'Text',
}

export enum GameEngineEvents {
  AddGameObject = 'AddGameObject',
  RegisterObjectUpdate = 'RegisterObjectUpdate',
  GameStarted = 'GameStarted',
}
