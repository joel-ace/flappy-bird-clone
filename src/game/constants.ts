import { Layout } from './enums';

export const DEFAULT_GAME_PROPERTIES = {
  velocityX: -2,
  velocityY: 0,
  fallSpeed: 0.4,
  width: Layout.Width,
  height: Layout.Height,
}

export const SOIL_HEIGHT = 120;
